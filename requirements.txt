# Deliberately NOT markitdown[all]: the all extra pulls cloud-audio
# transcription (speechrecognition/pydub), YouTube fetching, and Azure Document
# Intelligence — capabilities this offline tool must not ship. Runtime also
# enforces an extension allowlist (CONVERTIBLE_EXTENSIONS in backend/converter.py).
markitdown[docx,pdf,pptx,xlsx,xls,outlook]
customtkinter
tkinterdnd2
PyInstaller
spacy

# On-device OCR (opt-in at runtime): PP-OCRv4 ONNX models ship inside the
# rapidocr wheel, so nothing is downloaded at run time. pypdfium2 rasterizes
# scanned PDF pages. onnxruntime is already present via magika.
rapidocr-onnxruntime
pypdfium2
